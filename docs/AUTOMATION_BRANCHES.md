# Automation Branches

`main` is the production source of truth.

`automation/content-candidate` contains the latest validated generated-content candidate. It is replaceable and must never be treated as a durable release branch.

`codex/harden-content-publishing` contains the workflow and governance change that establishes this operating model.
